#1. 사용자에게 로또를 몇 개 살 건지 숫자 입력
#2. 1부터 45까지 숫자 중 6개를 랜덤으로 뽑기
#3. 사용자에게 입력받은 개수만큼 반복해서 뽑기
#4. 오름차순으로 정렬해서 출력하기

import random
N = int(input("로또 몇 개 사실건가요? : "))
for i in range(N):
    ans = random.sample(range(1,45),6)
    print(sorted(ans))